- if user_signed_in?
  %h3
    Hey,
    = current_user.username
  .form-group
    = form_tag(posts_path, method: "post", remote: true) do
      %input.form-control.box_post{maxlength:'140' ,name: "message", placeholder: "What are you thinking about?"}

#posts
  = render partial: 'posts/post', collection: @last_posts

