require 'rails_helper'

RSpec.describe "User Factory" do

  ## Test each factory
  #xit "is valid" do
    ##factory = FactoryGirl.build(:user)
    ##if factory.respond_to?(:valid?)
      ##expect(factory).to be_valid
    ##end
  #end

  #it "is valid" do
    #factory = FactoryGirl.build(:user, :with_posts)
    #if factory.respond_to?(:valid?)
      #expect(factory).to be_valid
    #end
  #end
end
